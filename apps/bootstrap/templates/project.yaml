---
apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: {{ .Values.name }}
  namespace: argocd
spec:
  description: {{ .Values.name }} project
  sourceRepos:
  - '*'
  sourceNamespaces:
  - '*'
  destinations:
  - name: {{ .Values.name }}
    namespace: '*'
  clusterResourceWhitelist:
  - group: '*'
    kind: '*'
