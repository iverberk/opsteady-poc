apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.cluster }}
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: cluster
    name: {{ .Values.cluster }}
type: Opaque
stringData:
  name: {{ .Values.cluster }}
  server: https://kubernetes.default.svc
  config: |-
    {
      "tlsClientConfig":{
        "insecure":false
      }
    }

